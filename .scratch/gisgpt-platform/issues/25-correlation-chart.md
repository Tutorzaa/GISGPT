# 25 — กราฟ correlation บนเว็บ (ECharts)

**What to build:** เมื่อผลมี chart (เช่น correlation hotspot↔met) หน้าเว็บวาดกราฟ scatter/เส้นจริงบนเบราว์เซอร์ (ECharts) ให้เห็น r/p + จุดข้อมูล — ปิดวงจร "พิสูจน์ผล" ใน UI

**Blocked by:** 18, 23

**Status:** ready-for-agent

- [ ] วาด scatter + เส้น fit + แสดง r/p
- [ ] รับ data จาก `/api/query` หรือ `/api/correlation`